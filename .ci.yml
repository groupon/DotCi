build:
 <% if (build.githubRepository.getPullRequest(1).listFiles().asList().contains(".ci.yml")) { %>
 run:  echo <%= build.githubRepository.getPullRequest(1).listFiles().asList() %>
 <%} else {%>
 run:  echo <%= build.githubRepository.getPullRequest(DOTCI_PULL_REQUEST.toInteger()).listFiles().asList().collect { it.filename } %>
 <%}%>

environment: 
  language: java

